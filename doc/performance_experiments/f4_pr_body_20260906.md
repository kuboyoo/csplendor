# 採用済みエンジン高速化：累積比較・受入・lint是正

## 状態

**BLOCKED（F4互換性検査）**。追加テスト5ファイルのlint違反7件は解消済み。
clean wheel隔離受入はPASS、remote CIはClang・Python 3.8・Windows互換性でFAILURE。
F4前半の作業branch push・PR作成・CI確認・隔離clean wheel受入のみ承認済み。
**mainへのmerge・常用環境への導入は未承認。自動mergeは有効にしない。**

## 統合範囲

base main `f5ec6c545c9a2727ca708bc4c6822daf07a2c4dc`。
計測・受入code `b202e6a0cbb2eded9bc2ee5e59f750428e73ca49`、
F1記録 `49878b661298bf45e39c5f5ca5afa6d0e363736a`。
前回F2/F3記録 `e486e27cbabdec4387f9d183a758720e1cf2caee`。
lint是正候補 `f6d28dde184ff1950c4729197a3a9ac1645b0c11` を含む
`review/f3-lint-correction-20260906`。以降はF4報告・証跡のみを追加予定。
lint是正後のsource digestとF1からの本体/build不変性は[是正報告](https://github.com/kuboyoo/csplendor/blob/review/f3-lint-correction-20260906/doc/performance_experiments/f3_lint_correction_20260906.md)に記録。

- 採用済みrule/solver/legacy MCTS/owning NumPy最適化と、そのreference/VERIFYを保持。
- main情報集合v2・frontierヒント互換修正はF1で統合済み。
- 4A保留。棄却案、Linux native/PGO、新TT、数値演算順変更は導入しない。
- README日英、未出荷変更履歴、再現・受入・復帰手順、圧縮/hash付き証跡。
- 外部AI/GUI source、モデル、学習データ、build/venv/.so、ユーザー未コミット変更は含めない。

## 効果（F1直接paired A/B）

portable code同士、22 pairs/11 blocksの独立再測定：

- exact reveal深さ7・100万node：2.373倍［95% CI 2.361–2.403］、current RSS約30%減。
- Python特徴量取得：12.808倍［12.514–13.048］。
- Python特徴量＋環境step：6.044倍［5.732–6.164］。

別workloadの数値。depth7はnode limit UNKNOWNで、7手詰め完全証明時間ではない。
Phase倍率を乗算せず、AI全体/問題保存速度へ外挿しない。
並列MCTSの累積高速化とLTO追加効果は未確定。native LTOは既定OFF。
Pythonの既存pybind11 LTOを新規効果と扱わない。

## 正しさ・受入

F1 source/build/binary identityを照合してnative44、Python595、reference/VERIFY、ASan証跡を再利用。
F2は隔離venv＋同じF1拡張でselfplay12/selfplay17実モデルCPU・float32受入。
実利用はPython MCTS＋V3/3133＋canonical/public313特徴。各2ply×400 sim、
実GUI handler各2手、詰み手への切替、warm session、NumPy/torch所有権を確認。
実GUI JSON-lines workerで5/7手の選択経路を終局まで再生し、訪問守備ノードの全合法応手を照合。
中断/再開/session既存3テスト、GUI bridge既存7テストも通過。

F3是正でCI lint/security lint/compileall、変更26件＋関連47件の計73件を再実行PASS。
Python 3.12の警告error＋coverage gateも595 passed、1 skipped、4 deselected、58.89%で通過。
assert/fixture/parameterization/skip/収集IDは維持、本番・build・計測fixtureは不変。

## 未解決・merge gate

- 候補固有CI lint7件は是正済み。旧失敗rawを保持し、新source digest／再検証rawを追加した。
- 初回hosted CIは9 SUCCESS・7 FAILURE・nightly適用外SKIP1。
  Clang/AppleClang constexpr上限（5 jobs）、Python 3.8計測driver（2 tests）、Windows slot fixture（4 tests）。
  原因・最小修正案はF4報告に記載。本体/build/既存test/計測コードは未修正。
- branch情報APIでは保護無効・強制required checksなし。適用rulesも空であるが、workflow全体を出荷gateとする。
- 外部dlsplendorはdirty。F2の実source hashは記録したが、そのHEADだけでは同一環境を再現できない。
  利用側の配備pin/常用Pythonはユーザー指定待ち。
- ブラウザ描画、GPU、旧Genbu（必要fileなし）、実モデルA/B/棋力、merge後の導入受入は未実施。
  候補のclean wheelによる隔離受入は今回PASS（常用installは不変更）。
- legacy未展開node主体ではRSS約18.6%増の既知tradeoff、soft deadline、未検証OSを保持。

## 導入・復帰

明示承認後に作業branch push→PR review/CI→PR経由merge。mainへ直接pushしない。
進んだmainや競合解消で本体/buildが変われば新treeの該当回帰・性能guardを追加。
merge SHAから別worktree/venvでclean wheelをbuild/installし、実.so identityと小さなconsumer受入を確認。
GUIのAI/mate両Pythonとroot/PYTHONPATH、利用側dependency pinだけを承認範囲で更新する。
旧venv/wheel/model/configは保持し、問題時は起動先を旧環境へ戻す。履歴resetやforce pushは使わない。

[最終レビュー](https://github.com/kuboyoo/csplendor/blob/review/f3-lint-correction-20260906/doc/performance_experiments/f2_f3_shipping_review_20260906.md)、[F1証跡](https://github.com/kuboyoo/csplendor/blob/review/f3-lint-correction-20260906/doc/performance_experiments/final_main_vs_candidate_20260906.md)、
[F4詳細・復帰](https://github.com/kuboyoo/csplendor/blob/review/f3-lint-correction-20260906/doc/performance_experiments/f4_integration_runbook_20260906.md)。
**F4前半で互換性ブロッカーを検出。merge・導入は承認待ち**。tag/Release/PyPI/branch削除も実施しない。

## F4前半の進捗

- origin/mainは上記baseと一致し、候補の祖先。新たな統合・競合解消なし。
- 初回CI：[run 34012764243](https://github.com/kuboyoo/csplendor/actions/runs/34012764243)、
  head=`f6d28dde184ff1950c4729197a3a9ac1645b0c11`、test merge=`1dda78202fb59aa72089450844f995291858983f`。
  Python 3.9–3.12、native、cpp-coverage、package、g++ strict、Linux arm64は成功。
  Python 3.12は594 passed / 2 skipped / 4 deselected、coverage 58.89%。
- clean wheel：`csplendor-0.1.0-cp312-cp312-linux_x86_64.whl`、
  SHA256=`575b9b6e4e8e8b75a67a2765bde434367a388ecd88d8855884c819f9caf21258`。
  Python3.12.1/GCC15.2/portable Release、native LTO OFF、既存pybind11 LTOを保持。
  wheel専用venvの実.so SHA256=`f2f653f4794d5454be82bce17ee4cb0ac38f9141fc7789e4275a7d01f01bb597`（F1と一致）。
  V3/通常・簡易payment、NumPy所有権、snapshot、既存7ケース（5/7 frontier・session・cancel等）PASS。
- 管理用protection APIはHTTP403だが、branch情報APIはprotected=false／enforcement offを返し、
  rules空・PR required checksなしも確認。検査gateの免除には使わない。
- [F4報告](https://github.com/kuboyoo/csplendor/blob/review/f3-lint-correction-20260906/doc/performance_experiments/f4_premerge_review_20260906.md)・
  [manifest](https://github.com/kuboyoo/csplendor/blob/review/f3-lint-correction-20260906/doc/performance_experiments/f4_premerge_manifest_20260906.json)。
  記録追加後の最終PR headとそのCI結果は、完了時に本PR本文へ別途追記する。
