# Phase 6 / §19–22 実施前契約

- 基準は5C-B完了 `3582909e7709d1c568e481c7ef965e41e1e62832`。別worktree
  `csplendor-build-profiles` / `perf/build-profiles`。元の詰め探索修正ブランチは統合・変更しない。
- engine sourceは変更せず、portable既定・Apple Silicon native・wheel guardを維持する。
- native、portable LTO、portable PGOをまず独立比較。各比較のsource/toolchainは同じ。
  pybind11暗黙LTOとnative executable LTOを別記する。PGO generate時間は採否に使わない。
- primary: `exact_reveal/hidden_reserve/depth7/1000000`、guard:
  `parallel_scheduler/five_moves/1T/20000`, `random_selfplay_apply/initial/20000`,
  `visible_solver/five_moves/100000`。採用にはprimary >=1.03、95% block CI下端>1、
  独立再測定で再現。主要guardの2%超回帰は独立再測定で判断する。
- smoke 4pairs、正式/再測定22pairs・11 fixed-slot crossover blocks、warmup2、CPU4、
  bootstrap10000。既存runnerを再利用し、build-profile差だけ明示opt-inで許容する。
  S0 digest/correctnessは厳守。非同期はS1 ledger/完了件数で評価し完全digest一致を要求しない。
- PGO trainingはseed17、initial/midgame_250/reveal_heavy/forced_pass/editor_fallback。
  legal count/codes、selfplay、exact/determinized MCTS、hidden情報、stop/failureを含める。
  holdoutはseed42、hidden_reserve/five_moves（trainingでは使わない）。GCC atomic counters、
  source/flags/compiler/profile SHA、generate/use別build、missing/mismatchはerror。
- 全native/Python/性能マーク、ASAN+UBSAN、並列TSan/failure-injection、wheel実buildを統合確認。
  perf/TSan不可はN/A、実NNモデルは読まない。Apple実機未検証は明記。
- §19–22は過去Phaseの記録を横断監査し、未計測を埋めたふりをしない。
  4A-1/2はユーザー判断で保留、棄却案の再実装なし。倍率を掛け合わせない。
- 中立/回帰案は自分の試作のみ撤去。記録は全てdoc下、build/profile/wheelはcommitしない。
  ユーザーの最新AGENTS指示に従い作業ブランチのみ通常pushし、次ticketへ自動で進まない。
